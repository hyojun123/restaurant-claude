name: restaurant_app
description: 아무거나 요정 - 뭐 먹을래? Flutter 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  geolocator: ^10.1.0
  permission_handler: ^11.0.1
  provider: ^6.1.1
  shared_preferences: ^2.2.0
  url_launcher: ^6.2.1
  flutter_naver_map: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/